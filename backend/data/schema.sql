CREATE TABLE IF NOT EXISTS food_facilities (
  locationId               INTEGER PRIMARY KEY,      -- dataset key (you used lowercase 'id' in TS)
  applicant                TEXT,
  facilityType             TEXT,
  cnn                      INTEGER,
  locationDescription      TEXT,
  address                  TEXT,
  block                    TEXT,
  lot                      TEXT,
  blocklot                 TEXT,
  permit                   TEXT,
  status                   TEXT,
  foodItems                TEXT,
  x                        REAL,
  y                        REAL,
  latitude                 REAL,
  longitude                REAL,
  schedule                 TEXT,
  dayshours                TEXT,
  noiSent                  TEXT,
  approved                 TEXT,
  received                 TEXT,
  priorPermit              INTEGER,
  expirationDate           TEXT,
  location                 TEXT,
  firePreventionDistricts  INTEGER,
  policeDistricts          INTEGER,
  supervisorDistricts      INTEGER,
  zipCodes                 INTEGER,
  neighborhoodsOld         TEXT
);